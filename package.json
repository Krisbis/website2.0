{
  "name": "website2-0-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Writeup generator — converts markdown to HTML and updates project cards",
  "scripts": {
    "generate": "node generate.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
